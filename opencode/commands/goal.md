---
description: Set, check, or clear a completion goal the agent works toward autonomously
agent: build
---

/goal $ARGUMENTS — use the `goal_set` / `goal_status` / `goal_clear` tools; never simulate the goal.
