---
description: Run a prompt on a cadence or self-paced until a stop condition holds or the iteration cap is reached
agent: build
---

/loop $ARGUMENTS — use the `loop` tool with the user's words as `prompt`; never simulate the loop.
